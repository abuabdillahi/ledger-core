[project]
name = "ledger-core"
version = "0.1.0"
description = "In-memory append-only double-entry ledger core"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
